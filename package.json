{
  "name": "shepherd",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "bun --preload @opentui/solid/preload src/modules/cli/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "fmt": "oxfmt .",
    "lint": "oxlint .",
    "install:bin": "bun build.ts && install -m 755 dist/shepherd \"${SHEPHERD_BIN_DIR:-$HOME/.local/bin}/shepherd\" && echo \"installed: ${SHEPHERD_BIN_DIR:-$HOME/.local/bin}/shepherd\" && { \"${SHEPHERD_BIN_DIR:-$HOME/.local/bin}/shepherd\" daemon restart >/dev/null 2>&1 && echo 'daemon restarted'; true; }"
  },
  "dependencies": {
    "@opentui/core": "^0.5.10",
    "@opentui/solid": "^0.5.10",
    "solid-js": "^1.9.15",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^26",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typescript": "^7"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
